

```java
//源代码 
public int calc() {
        int a = 100;
        int b = 200;
        int c = 300;
        return (a + b) * c;
    }


//对应字节码解析
  public int calc();
    Code:
       0: bipush        100		//执行偏移地址为0的指令：将单字节的整型常量100推入操作数栈顶 bipush[-128,127]
       2: istore_1				//执行偏移地址为2的指令：将操作数栈顶的整型值100出栈并且存放到第1个局部变量槽中
       3: sipush        200		//执行偏移地址为3的指令：将单字节的整型常量200推入操作数栈顶
       6: istore_2				//执行偏移地址为6的指令：将单字节的整型常量200出栈并且存放到第2个局部变量槽中
       7: sipush        300		//执行偏移地址为7的指令：将单字节的整型常量300推入操作数栈顶
      10: istore_3				//执行偏移地址为10的指令：将单字节的整型常量300出栈并且存放到第3个局部变量槽中
      11: iload_1				//执行偏移地址为11的指令：将局部变量表第1个变量槽中的整型复制到栈顶
      12: iload_2				//执行偏移地址为12的指令：将局部变量表第2个变量槽中的整型复制到栈顶
      13: iadd					//执行偏移地址为13的指令：将栈中头两个元素出栈并且相加，然后重新入栈
      14: iload_3				//执行偏移地址为14的指令：将局部变量表第3个变量槽中的整型复制到栈顶
      15: imul					//执行偏移地址为15的指令：将栈中头两个元素出栈并且相乘，然后重新入栈
      16: ireturn				//执行偏移地址为16的指令：结束方法，并将栈顶的整型值返回给方法的调用者

```





```java
 public int test() {
        int a = 1;
        for (int i = 0; i < 4; i++) {
            a = a + 1;
        }

        if (a > 3) {
            return a;
        } else {
            return a - 1;
        }
    }


iinc指令：用于对本地(局部)变量进行自增减操作，该指令第一参数为本地变量的编号，第二个参数为自增减的数量。


  public int test();
    Code:
       0: iconst_1				//将常量1压栈
       1: istore_1				//将栈中常量1弹栈，并且保存到第1个局部变量槽中
       2: iconst_0				//将常量0压栈
       3: istore_2				//将栈中常量0弹栈，并且保存在第2个局部变量槽中
       4: iload_2				//将局部变量表中第2个槽中的变量压栈
       5: iconst_4				//将常量4压栈
       6: if_icmpge     19		//比较栈顶两个元素大小 即4和0
       9: iload_1				//将局部变量表中第1个槽中的变量压栈 即1
      10: iconst_1				//将常量1压栈
      11: iadd					//将栈顶两整型弹栈并且相加，然后压栈 此时栈顶为2
      12: istore_1				//栈顶整型2弹栈，并且保存到局部变量表的第1个变量槽中
      13: iinc          2, 1	//对局部变量表中槽2的变量执行加1操作
      16: goto          4		//跳到执行偏移地址为4的指令处
      19: iload_1				//循环执行完毕 将局部变量表中第1个槽中的变量压栈
      20: iconst_3				//将变量3压栈
      21: if_icmple     26		//比较栈顶两整型大小 即3和槽1变量
      24: iload_1				//将局部变量表中第1个槽中的变量压栈
      25: ireturn				//结束方法，并将栈顶的整型值返回给方法的调用者
      26: iload_1				//将局部变量表中第1个槽中的变量压栈
      27: iconst_1				//将产量1压栈
      28: isub					//栈顶两int型弹栈，较栈顶减去下面int型，将结果压栈
      29: ireturn				//结束方法，并将栈顶的整型值返回给方法的调用者
}
```

